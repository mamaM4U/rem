name: shared_models
description: Shared models and DTOs between arona, plana, and api_server
version: 1.0.0
publish_to: none

environment:
  sdk: ^3.9.0

resolution: workspace

dependencies:
  json_annotation: ^4.9.0

dev_dependencies:
  build_runner: ^2.11.0
  json_serializable: ^6.11.2
